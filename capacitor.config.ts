import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'sz.edulink.prototype',
  appName: 'EduLink Eswatini',
  webDir: 'dist',
  bundledWebRuntime: false,
  android: {
    backgroundColor: '#f4f7fb'
  }
};

export default config;
