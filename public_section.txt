
function Home({go}){
 const slides=[
  {image:'./school-people.jpg',eyebrow:'WELCOME TO KA-SHALI',title:'Quality education. Strong character. Bright futures.',text:'A Christian school community where learners are encouraged to grow in knowledge, confidence, discipline and purpose.'},
  {image:'./gallery/school-group-1.jpg',eyebrow:'OUR SCHOOL COMMUNITY',title:'A place where learners grow together.',text:'School life is about learning, friendship, responsibility and becoming ready for the opportunities ahead.'},
  {image:'./gallery/school-group-2.jpg',eyebrow:'ACHIEVEMENT & PARTICIPATION',title:'We celebrate effort, talent and achievement.',text:'Learners are encouraged to participate, discover their strengths and take pride in their progress.'},
  {image:'./gallery/school-group-3.jpg',eyebrow:'LEARN • GROW • SERVE',title:'Learning extends beyond the classroom.',text:'A school community that values participation, teamwork, leadership and positive contribution.'},
  {image:'./gallery/school-group-4.jpg',eyebrow:'KA-SHALI PRO-LEARNING',title:'Preparing learners for tomorrow.',text:'A school experience built around quality education and the values expressed in our school motto.'}
 ];
 const [slide,setSlide]=useState(0);
 useEffect(()=>{const t=setInterval(()=>setSlide(x=>(x+1)%slides.length),6000);return()=>clearInterval(t)},[]);
 const current=slides[slide];
 return <main className="ksProfessionalHome">
  <section className="ksHeroPro" aria-label="School highlights">
   {slides.map((item,i)=><div key={item.image} className={'ksHeroSlide '+(i===slide?'active':'')}><img src={item.image} alt="Ka-Shali Pro-Learning Christian Academy learners"/><div className="ksHeroOverlay"/></div>)}
   <button className="ksHeroArrow left" onClick={()=>setSlide(x=>(x-1+slides.length)%slides.length)} aria-label="Previous slide"><ChevronLeft size={25}/></button><button className="ksHeroArrow right" onClick={()=>setSlide(x=>(x+1)%slides.length)} aria-label="Next slide"><ChevronRight size={25}/></button>
   <div className="ksHeroContent"><span className="ksHeroEyebrow">{current.eyebrow}</span><h1>{current.title}</h1><p>{current.text}</p><div className="ksHeroButtons"><button className="ksGoldBtn" onClick={()=>go('register')}>Enrol at Ka-Shali <ChevronRight size={17}/></button><button className="ksOutlineBtn" onClick={()=>go('find')}>Discover our school <ChevronRight size={17}/></button></div></div>
   <div className="ksHeroBadge"><img src="./school-logo.png" alt={SCHOOL_NAME}/></div><div className="ksHeroDots">{slides.map((_,i)=><button key={i} className={i===slide?'active':''} onClick={()=>setSlide(i)} aria-label={'Show slide '+(i+1)}/>)}</div>
  </section>
  <section className="ksWelcomeSection"><div className="ksSectionIntro"><span className="ksEyebrow">WELCOME TO KA-SHALI</span><h2>A school that prepares learners for <em>life.</em></h2><p>Choosing a school is about more than a classroom. Ka-Shali Pro-Learning Christian Academy presents a learning environment centred on quality education, Christian values, character and a sense of community.</p></div><div className="ksReasonGrid"><article><div className="ksReasonIcon"><GraduationCap/></div><h3>Quality Education</h3><p>Build a strong academic foundation through purposeful learning and continued progress.</p></article><article><div className="ksReasonIcon"><ShieldCheck/></div><h3>Christian Values</h3><p>Encourage discipline, integrity, responsibility and values that guide learners beyond school.</p></article><article><div className="ksReasonIcon"><Users/></div><h3>School Community</h3><p>Give learners opportunities to participate, connect, grow in confidence and contribute.</p></article></div></section>
  <section className="ksAboutHome"><div className="ksAboutImage"><img src="./gallery/school-group-1.jpg" alt="Ka-Shali Pro-Learning Christian Academy school community"/></div><div className="ksAboutCopy"><span className="ksEyebrow">ABOUT THE SCHOOL</span><h2>More than a school. A community for growth.</h2><p>Ka-Shali Pro-Learning Christian Academy is presented through its people, its learning community and its commitment to quality education. Explore the school information, programmes and admissions options before choosing the next step for your child.</p><div className="ksAboutActions"><button className="ksDarkBtn" onClick={()=>go('find')}>About the school <ChevronRight size={17}/></button><button className="ksTextBtn" onClick={()=>go('courses')}>View courses & classes <ChevronRight size={17}/></button></div></div></section>
  <section className="ksValuesBand"><div><span className="ksEyebrow light">OUR SCHOOL MOTTO</span><h2>“Quality education in the house of God.”</h2><p>The motto carried by the school badge is at the heart of the school's identity.</p></div><img src="./school-logo.png" alt="Ka-Shali Pro-Learning Christian Academy badge"/></section>
  <section className="ksEnrolCta"><div><span className="ksEyebrow light">TAKE THE NEXT STEP</span><h2>Give your child a place to learn, grow and serve.</h2><p>Explore the school information or start the registration process through the website.</p></div><div className="ksCtaButtons"><button className="ksGoldBtn" onClick={()=>go('register')}>Register <ChevronRight size={17}/></button><button className="ksOutlineBtn" onClick={()=>go('find')}>School information <ChevronRight size={17}/></button></div></section>
  <section id="ksContact" className="ksContactPro"><div className="ksContactTitle"><span className="ksEyebrow light">CONTACT THE SCHOOL</span><h2>{SCHOOL_NAME}</h2><p>P.O. Box 1747, Manzini, Eswatini</p></div><div className="ksContactLinks"><a href="https://wa.me/26879118139" target="_blank" rel="noreferrer"><span className="ksContactIcon whatsapp"><MessageCircle/></span><span><small>WHATSAPP / CALL</small><b>{SCHOOL_PHONE_1}</b></span></a><a href="https://wa.me/26876118139" target="_blank" rel="noreferrer"><span className="ksContactIcon whatsapp"><MessageCircle/></span><span><small>WHATSAPP / CALL</small><b>{SCHOOL_PHONE_2}</b></span></a><a href={`mailto:${SCHOOL_EMAIL}`}><span className="ksContactIcon email"><Mail/></span><span><small>EMAIL</small><b>{SCHOOL_EMAIL}</b></span></a></div></section>
 </main>
}

function SchoolProfile({school,onBack,onLogin}){
 const s=school||seedSchool;
 const levels=(s.levels||[]).flatMap(l=>l.classes||[]);
 return <main className="publicInfoPage"><section className="publicInfoHero"><button className="backBtn" onClick={onBack}>← Back to home</button><div className="publicInfoHeroInner"><img src="./school-logo.png" alt={SCHOOL_NAME}/><div><span className="ksEyebrow light">ABOUT THE SCHOOL</span><h1>{SCHOOL_NAME}</h1><p>Quality education in the house of God — a school community focused on learning, character, participation and preparation for the future.</p></div></div></section><section className="publicInfoBody"><div className="publicInfoCopy"><span className="ksEyebrow">OUR SCHOOL</span><h2>A place to learn, grow and serve.</h2><p>Ka-Shali Pro-Learning Christian Academy provides a structured learning environment for learners and families. The school website gives prospective parents access to school information, programmes, contact details and the online enrolment process.</p><div className="publicInfoCards"><article><GraduationCap/><b>Academic growth</b><span>Programmes across the available school levels and subjects.</span></article><article><ShieldCheck/><b>Christian values</b><span>Character, discipline, responsibility and service.</span></article><article><Users/><b>Community</b><span>A school environment where learners participate and grow together.</span></article></div></div><aside className="publicInfoSide"><h3>School information</h3><p><b>Location</b><br/>{s.centre||'Manzini, Eswatini'}</p><p><b>Programmes / classes</b><br/>{levels.length||PROGRAMS.length} available options</p><button className="ksDarkBtn" onClick={onLogin}>Login to the school portal <ChevronRight size={17}/></button></aside></section></main>
}
function CoursesClasses({go}){
 return <main className="coursesPage"><section className="coursesHero"><button className="backBtn" onClick={()=>go('home')}>← Back to home</button><span className="ksEyebrow light">PROGRAMMES & COURSES</span><h1>Learning options at Ka-Shali.</h1><p>Explore the programmes and class options presented by the school before starting an enrolment application.</p></section><section className="coursesTableCard"><div className="coursesIntro"><span className="ksEyebrow">COURSE DIRECTORY</span><h2>Programmes available</h2></div><div className="tableWrap"><table><thead><tr><th>Programme</th><th>Level / category</th><th>Subjects</th><th>Fee</th></tr></thead><tbody>{PROGRAMS.map(p=><tr key={p.id}><td><b>{p.label}</b><small>{p.category}</small></td><td>{p.level}</td><td>{(p.subjects||[]).join(', ')}</td><td>{p.fee==null?'Contact school':'E '+Number(p.fee).toLocaleString()}</td></tr>)}</tbody></table></div><div className="coursesBottomActions"><button className="ksDarkBtn" onClick={()=>go('register')}>Start registration <ChevronRight size={17}/></button><button className="ksTextBtn" onClick={()=>go('find')}>About the school <ChevronRight size={17}/></button></div></section></main>
}
function GalleryPage({go}){
 const photos=[
  ['./school-people.jpg','Welcome to Ka-Shali','Our school community'],
  ['./gallery/school-group-1.jpg','Our learners','Learning and growing together'],
  ['./gallery/school-group-2.jpg','Achievement and participation','Celebrating learner effort and teamwork'],
  ['./gallery/school-group-3.jpg','School community','Learning, leadership and friendship'],
  ['./gallery/school-group-4.jpg','Ka-Shali identity','Quality education and school values']
 ];
 return <main className="galleryPage"><section className="galleryHero"><button className="backBtn" onClick={()=>go('home')}>← Back to home</button><span className="ksEyebrow light">GALLERY</span><h1>Life at Ka-Shali.</h1><p>Explore photographs from the school website and see the people and community behind the school.</p></section><section className="galleryGridPro">{photos.map(([src,title,text])=><figure key={src}><img src={src} alt={title}/><figcaption><b>{title}</b><span>{text}</span></figcaption></figure>)}</section></main>
}
function PublicFooter({go}){return <footer className="ksSiteFooter"><div className="ksFooterContact"><a href="https://wa.me/26879118139" target="_blank" rel="noreferrer"><MessageCircle/><span><b>WhatsApp</b><small>Quick school enquiries</small></span></a><a href={`mailto:${SCHOOL_EMAIL}`}><Mail/><span><b>Email</b><small>{SCHOOL_EMAIL}</small></span></a></div><div className="ksFooterBrand"><img src="./school-logo.png" alt={SCHOOL_NAME}/><strong>{SCHOOL_NAME}</strong><span>School learning platform • EduLink</span></div><div className="ksFooterLinks"><button onClick={()=>window.scrollTo({top:0,behavior:'smooth'})}>Back to top ↑</button>{go&&<button onClick={()=>go('login')}>Admin / Portal Login</button>}<small>Private school application • Online school learning platform</small></div></footer>}

function PublicFormPage({title,onBack,children}){return <div className="public publicFull"><FlagWatermark/><header className="publicHeader"><button className="backBtn" onClick={onBack}>← Back</button><Logo/><span className="offlineBadge">WEB PORTAL</span></header><main className="fullFormWrap"><div className="fullFormCard"><span className="eyebrow">{SCHOOL_NAME}</span><h1>{title}</h1>{children}</div></main><PublicFooter/></div>}

function LoginForm({data,setData,onLogin}){const [role,setRole]=useState('Student');const [email,setEmail]=useState('');const [pw,setPw]=useState('');const [err,setErr]=useState('');const [forgot,setForgot]=useState(false);const [recovery,setRecovery]=useState({identifier:'',question:RECOVERY_QUESTIONS[0],answer:'',newPassword:'',confirm:''});const [recoveryMsg,setRecoveryMsg]=useState('');function submit(){const key=email.trim().toLowerCase();const approved=data.pendingUsers.find(x=>(((x.email||'').toLowerCase()===key)||((x.username||'').toLowerCase()===key))&&x.status==='APPROVED'&&x.role===role);const staff=data.schools.flatMap(s=>s.staff||[]).find(x=>((x.email||'').toLowerCase()===key)&&x.role===role);const student=data.students.find(x=>((x.email||'').toLowerCase()===key)&&role==='Student');const a=(key==='leo3mnisi'&&pw==='Leo76676728'&&role==='System Admin')?{username:'Leo3Mnisi',password:'Leo76676728',role:'System Admin',name:'Leo Mnisi',email:'admin@edulink.local',id:'ADMIN'}:approved||staff&&data.pendingUsers.find(x=>x.id===staff.id)||student&&data.pendingUsers.find(x=>x.id===student.userId);if(!a||a.password!==pw)return setErr('Username/email, role or password is incorrect, or the account is awaiting approval.');onLogin({...a,schoolId:SCHOOL_ID})}function recover(){setRecoveryMsg('');const key=recovery.identifier.trim().toLowerCase();const u=data.pendingUsers.find(x=>((x.email||'').toLowerCase()===key||(x.username||'').toLowerCase()===key)&&x.status==='APPROVED');if(!key||!recovery.answer.trim()||!recovery.newPassword||recovery.newPassword!==recovery.confirm)return setRecoveryMsg('Enter your username/email, recovery answer and matching new password.');if(!u)return setRecoveryMsg('The username/email does not match an approved registration.');if((u.recoveryQuestion||'')!==recovery.question||String(u.recoveryAnswer||'').trim().toLowerCase()!==recovery.answer.trim().toLowerCase())return setRecoveryMsg('The recovery question or answer does not match the registration.');const users=data.pendingUsers.map(x=>x.id===u.id?{...x,password:recovery.newPassword}:x);setData({...data,pendingUsers:users});setRecoveryMsg('Password changed successfully. You can now log in.')}if(forgot)return <><span className="eyebrow">ACCOUNT RECOVERY</span><h2>Forgot your password?</h2><p className="muted">Use the recovery question and answer you selected when registering.</p><label>Username or Email<input value={recovery.identifier} onChange={e=>setRecovery({...recovery,identifier:e.target.value})}/></label><label>Recovery question<select value={recovery.question} onChange={e=>setRecovery({...recovery,question:e.target.value})}>{RECOVERY_QUESTIONS.map(q=><option key={q}>{q}</option>)}</select></label><label>Recovery answer<input value={recovery.answer} onChange={e=>setRecovery({...recovery,answer:e.target.value})}/></label><label>New Password<input type="password" value={recovery.newPassword} onChange={e=>setRecovery({...recovery,newPassword:e.target.value})}/></label><label>Confirm New Password<input type="password" value={recovery.confirm} onChange={e=>setRecovery({...recovery,confirm:e.target.value})}/></label><button className="primary full large" onClick={recover}>Create New Password <ChevronRight size={17}/></button><button className="outline full" onClick={()=>setForgot(false)}>Back to Login</button>{recoveryMsg&&<div className={recoveryMsg.includes('successfully')?'successBox':'error'}>{recoveryMsg}</div>}</>;return <><div className="roleSelectGrid">{Object.keys(ROLES).map(r=><button key={r} className={role===r?'roleChoice active':'roleChoice'} onClick={()=>setRole(r)}>{r}</button>)}</div><label>Username or Email<input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Username or email"/></label><label>Password<input type="password" value={pw} onChange={e=>setPw(e.target.value)}/></label><button className="primary full large" onClick={submit}>Login to School <ChevronRight size={17}/></button><button className="linkButton" type="button" onClick={()=>{setForgot(true);setErr('')}}>Forgot Password?</button>{err&&<div className="error">{err}</div>}</>}
function RegisterForm({data,setData,onDone}){const [role,setRole]=useState('Student');const [f,setF]=useState({name:'',surname:'',username:'',email:'',phone:'',idNumber:'',schoolId:SCHOOL_ID,className:'',password:'',confirm:'',idDocument:'',photo:'',recoveryQuestion:RECOVERY_QUESTIONS[0],recoveryAnswer:''});const [msg,setMsg]=useState('');const [done,setDone]=useState(false);const set=(k,v)=>setF(x=>({...x,[k]:v}));function submit(){const idRequired=role!=='Student';if(!f.name||!f.surname||!f.email||(idRequired&&!f.idNumber)||!f.password||f.password!==f.confirm||!f.recoveryQuestion||!f.recoveryAnswer.trim())return setMsg('Complete the required fields, recovery question and recovery answer, and make sure both passwords match.');if(role==='Student'&&!f.className)return setMsg('Select the programme/class.');if(role==='Student'){const matches=(data.applications||[]).filter(a=>a.status==='APPROVED'&&a.className===f.className&&String(a.childName||'').trim().toLowerCase()===f.name.trim().toLowerCase()&&String(a.childSurname||'').trim().toLowerCase()===f.surname.trim().toLowerCase());if(!matches.length)return setMsg('Student registration requires an approved parent application for this learner and class.');if(matches.length>1)return setMsg('More than one approved application matches this learner. Please contact the school.');}if(role!=='Student'&&role!=='Parent'&&!f.username)return setMsg('Choose a username.');if(role!=='Student'&&role!=='Parent'&&(!f.idDocument||!f.photo))return setMsg('Staff registration requires an ID document and photo.');const emailKey=f.email.trim().toLowerCase();const usernameKey=f.username.trim().toLowerCase();const emailExists=data.pendingUsers.some(u=>emailKey && (u.email||'').trim().toLowerCase()===emailKey);const usernameExists=usernameKey && data.pendingUsers.some(u=>(u.username||'').trim().toLowerCase()===usernameKey);if(emailExists||usernameExists)return setMsg('That email or username is already registered.');const approvedStudentApp=role==='Student'?(data.applications||[]).find(a=>a.status==='APPROVED'&&a.className===f.className&&String(a.childName||'').trim().toLowerCase()===f.name.trim().toLowerCase()&&String(a.childSurname||'').trim().toLowerCase()===f.surname.trim().toLowerCase()):null;const u={...f,username:role==='Student'?f.email.split('@')[0]:f.username,recoveryAnswer:f.recoveryAnswer.trim().toLowerCase(),applicationId:approvedStudentApp?.id||'',id:uid('USR'),schoolId:SCHOOL_ID,role,status:'PENDING'};setData(prev=>({...prev,pendingUsers:[...prev.pendingUsers,u],notifications:[...prev.notifications,{id:uid('N'),title:'New registration awaiting approval',body:`${f.name} ${f.surname} registered as ${role}.`,audience:'admin'}]}));setDone(true)}if(done)return <><div className="successBox"><CheckCircle/><div><b>Registration submitted.</b><p>Your account will be reviewed by an authorized school approver before you can log in.</p></div></div><button className="primary full" onClick={onDone}>Return to home</button></>;return <><div className="roleSelectGrid">{Object.keys(ROLES).filter(r=>r!=='System Admin').map(r=><button key={r} className={role===r?'roleChoice active':'roleChoice'} onClick={()=>setRole(r)}>{r}</button>)}</div><div className="formGrid"><label>First name*<input value={f.name} onChange={e=>set('name',e.target.value)}/></label><label>Surname*<input value={f.surname} onChange={e=>set('surname',e.target.value)}/></label>{role!=='Student'&&<label>Username*<input value={f.username} onChange={e=>set('username',e.target.value)}/></label>}<label>Email*<input type="email" value={f.email} onChange={e=>set('email',e.target.value)}/></label><label>{role==='Student'?'ID Number (optional)':'ID Number*'}<input value={f.idNumber} onChange={e=>set('idNumber',e.target.value)} placeholder={role==='Student'?'Optional for young learners':''}/></label><label>Mobile phone<input value={f.phone} onChange={e=>set('phone',e.target.value)}/></label>{role==='Student'&&<label>Class / Form*<select value={f.className} onChange={e=>set('className',e.target.value)}><option value="">Select class</option>{flattenClasses(data.schools[0]).map(c=><option key={c.name} value={c.name}>{c.name}</option>)}</select></label>}{role!=='Student'&&role!=='Parent'&&<><FileCapture label="ID document" accept="image/*,.pdf" value={f.idDocument} onValue={v=>set('idDocument',v)} camera/><FileCapture label="Photo" accept="image/*" value={f.photo} onValue={v=>set('photo',v)} camera/></>}<label>Recovery question*<select value={f.recoveryQuestion} onChange={e=>set('recoveryQuestion',e.target.value)}>{RECOVERY_QUESTIONS.map(q=><option key={q}>{q}</option>)}</select></label><label>Recovery answer*<input value={f.recoveryAnswer} onChange={e=>set('recoveryAnswer',e.target.value)} placeholder="Enter an answer you will remember"/></label><label>Password*<input type="password" value={f.password} onChange={e=>set('password',e.target.value)}/></label><label>Confirm password*<input type="password" value={f.confirm} onChange={e=>set('confirm',e.target.value)}/></label></div><button className="primary full" onClick={submit}>Submit registration for approval</button>{msg&&<div className="error">{msg}</div>}</>}
async function persistLocalFile(value){return value}
function compressImage(file,maxSize=1400,quality=.78){return new Promise(resolve=>{if(!file?.type?.startsWith('image/'))return resolve(null);const img=new Image();const reader=new FileReader();reader.onload=()=>{img.onload=()=>{const scale=Math.min(1,maxSize/Math.max(img.width,img.height));const c=document.createElement('canvas');c.width=Math.max(1,Math.round(img.width*scale));c.height=Math.max(1,Math.round(img.height*scale));c.getContext('2d').drawImage(img,0,0,c.width,c.height);resolve(c.toDataURL('image/jpeg',quality))};img.src=reader.result};reader.readAsDataURL(file)})}
function FileCapture({label,accept='image/*,.pdf',value,onValue,camera=false}){const cameraInput=useRef();const fileInput=useRef();async function readFile(file){if(!file)return;const compressed=await compressImage(file);if(compressed)return onValue(compressed);const reader=new FileReader();reader.onload=()=>onValue(reader.result);reader.readAsDataURL(file)}return <label className="uploadField">{label}<div className="scanField"><div className="fileStatus">{value?<><Paperclip size={15}/><span>{value.startsWith('data:image')?'Image captured / selected':'Document captured / selected'}</span></>:<span>Nothing attached yet</span>}</div><div className="scanActions">{camera&&<button type="button" className="captureBtn" onClick={()=>cameraInput.current?.click()}><Camera size={15}/> Camera</button>}<button type="button" className="captureBtn" onClick={()=>fileInput.current?.click()}><Upload size={15}/> Choose</button></div><input ref={cameraInput} className="fileOverlay" type="file" accept="image/*" capture="environment" onChange={e=>{readFile(e.target.files?.[0]);e.target.value=''}}/><input ref={fileInput} className="fileOverlay" type="file" accept={accept} onChange={e=>{readFile(e.target.files?.[0]);e.target.value=''}}/></div></label>}
function DocumentViewer({title,items,onClose}){const list=(Array.isArray(items)?items:items?[items]:[]).filter(Boolean);const [index,setIndex]=useState(0);if(!list.length)return null;const item=list[index];const isImage=item.startsWith('data:image')||item.match(/\.(png|jpe?g|webp)$/i);return <div className="documentViewer" role="dialog" aria-modal="true"><div className="documentViewerCard"><div className="documentViewerHead"><div><span className="eyebrow">SCHOOL DOCUMENT</span><h2>{title}</h2><small>Document {index+1} of {list.length}</small></div><button className="close" onClick={onClose}><X/></button></div><div className="documentPreview">{isImage?<img src={item} alt={title}/>:<iframe title={title} src={item}/>}</div><div className="documentViewerActions">{list.length>1&&<><button className="outline" disabled={index===0} onClick={()=>setIndex(i=>i-1)}>Previous</button><button className="outline" disabled={index===list.length-1} onClick={()=>setIndex(i=>i+1)}>Next</button></>}<button className="primary" onClick={onClose}>Close document</button></div></div></div>}

function Profile({user,data,setData}){const staff=data.schools.flatMap(s=>s.staff||[]).find(st=>st.email===user.email);const current=data.pendingUsers.find(u=>u.id===user.id)||{...user,...staff};const [photo,setPhoto]=useState(current.photo||staff?.photo||'');const [details,setDetails]=useState({email:current.email||'',phone:current.phone||'',idNumber:current.idNumber||''});const [pw,setPw]=useState({current:'',newPassword:'',confirm:''});const [detailPw,setDetailPw]=useState('');const [msg,setMsg]=useState('');const [detailMsg,setDetailMsg]=useState('');const photoInput=useRef();function updateUser(patch){const users=data.pendingUsers.map(u=>u.id===current.id?{...u,...patch}:u);const schools=data.schools.map(s=>({...s,staff:(s.staff||[]).map(st=>st.email===current.email?{...st,...patch}:st)}));setData({...data,pendingUsers:users,schools})}async function file(e){const file=e.target.files?.[0];if(!file)return;const compressed=await compressImage(file,1000,.8);if(compressed){setPhoto(compressed);updateUser({photo:compressed})}else{const r=new FileReader();r.onload=()=>{setPhoto(r.result);updateUser({photo:r.result})};r.readAsDataURL(file)}e.target.value=''}function changePassword(){setMsg('');if(pw.current!==current.password)return setMsg('Current password is incorrect.');if(!pw.newPassword||pw.newPassword!==pw.confirm)return setMsg('Enter a matching new password.');updateUser({password:pw.newPassword});setPw({current:'',newPassword:'',confirm:''});setMsg('Password changed successfully.')}function changeDetails(){setDetailMsg('');if(detailPw!==current.password)return setDetailMsg('Current password is incorrect.');if(!details.email)return setDetailMsg('Email is required.');updateUser(details);setDetailPw('');setDetailMsg('Profile details updated successfully.')}return <><HeaderBlock eyebrow="PROFILE" title="My Profile" text="Manage your account details, profile photo and security settings."/><div className="profileSettingsGrid profileSettingsPro"><Panel title="Profile photo"><div className="profilePhotoBox">{photo?<img src={photo} alt="Profile"/>:<div className="profilePhotoPlaceholder"><UserRound size={52}/></div>}<div className="profilePhotoMeta"><b>{current.name} {current.surname}</b><span>{current.role}</span></div><button className="outline" onClick={()=>photoInput.current?.click()}><Camera size={16}/> Change photo</button><input ref={photoInput} className="fileOverlay" type="file" accept="image/*" capture="user" onChange={file}/></div></Panel><Panel title="Change password"><div className="formStack"><label>Current password<input type="password" value={pw.current} onChange={e=>setPw({...pw,current:e.target.value})}/></label><label>New password<input type="password" value={pw.newPassword} onChange={e=>setPw({...pw,newPassword:e.target.value})}/></label><label>Confirm new password<input type="password" value={pw.confirm} onChange={e=>setPw({...pw,confirm:e.target.value})}/></label></div><button className="primary" onClick={changePassword}><LockKeyhole size={16}/> Change password</button>{msg&&<div className={msg.includes('successfully')?'successBox':'error'}>{msg}</div>}</Panel><Panel title="Change account details"><div className="formStack"><label>Email<input value={details.email} onChange={e=>setDetails({...details,email:e.target.value})}/></label><label>Mobile phone<input value={details.phone} onChange={e=>setDetails({...details,phone:e.target.value})}/></label><label>ID Number<input value={details.idNumber} onChange={e=>setDetails({...details,idNumber:e.target.value})}/></label><label>Current password to verify<input type="password" value={detailPw} onChange={e=>setDetailPw(e.target.value)} placeholder="Required for security"/></label></div><button className="primary" onClick={changeDetails}><ShieldCheck size={16}/> Save verified changes</button>{detailMsg&&<div className={detailMsg.includes('successfully')?'successBox':'error'}>{detailMsg}</div>}</Panel></div></>}

